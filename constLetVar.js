var name = 'Dabaa';
if(name.length > 4) {
    name = 'Jaba'
}
console.log(name);

// if any variable value will can change then it have to write by var / let

// or if any variable value will not possible(sure) to change then it have to write by const
